PROJECT(FACE_LANDMARK)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
FIND_PACKAGE( OpenCV REQUIRED )
AUX_SOURCE_DIRECTORY(. DIR_SRCS)
ADD_EXECUTABLE(FACE_LANDMARK ${DIR_SRCS})
TARGET_LINK_LIBRARIES(FACE_LANDMARK ${OpenCV_LIBS})